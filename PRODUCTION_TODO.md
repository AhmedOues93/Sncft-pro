# Production TODO

- Add auth/authorization for admin endpoints.
- Persist imports in Supabase (currently in-memory runtime store).
- Add observability, rate-limiting, retry, and background jobs.
- Improve admin UI (file upload + publish/rollback confirmations).
- Add complete Flutter API wiring + state management.
